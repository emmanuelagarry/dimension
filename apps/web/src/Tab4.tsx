const Tab4 = () => {
  return (
    <>
      <p>This page further explains the application in the 3rd tab</p>

      <h2>Auth</h2>
      <p>For the authentication, there are two ways to authenticate</p>
      <p>One uses my email and password credentials and generates a token</p>
      <p>The other just generate a token for simplicity</p>
      <br></br>

      <p>
        In gereral a token mst be generated to use the application in the 3rd
        tab
      </p>

      <br></br>
      <p>
        I used vite to set up the project. When building the app, it will build
        SnderSender.tsx as a react component. This component can be deployed to
        npm for other developers to use.
      </p>
    </>
  );
};

export default Tab4;
